import React from 'react';
import styles from './BlockGrid.module.scss';

const BlockImg = () => {
  return (
    <div className={styles.BlockImg}>
      <img src='/hard-maple-endgrain-zoom.jpg' alt='' />
    </div>
  );
};

export default BlockImg;
